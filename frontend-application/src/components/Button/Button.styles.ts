import styled from "styled-components";


export const StyledButton = styled.button`
  padding: 4px 16px;
  min-width: 141px;
  height: 40px;

  /* Gradient / Primary */
  background: linear-gradient(180deg, #ff8b33 10%, #f56a00 90%);

  /* Elevation/Floor 1 */
  box-shadow: 0px 1px 2px rgba(0, 0, 0, 0.25);
  border-radius: 2px;
  font-weight: 700;
  font-size: 16px;
  line-height: 150%;
  text-align: center;

  /* Neutral/White */
  color: #ffffff;
`;
